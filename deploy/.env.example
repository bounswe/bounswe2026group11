# Fill in and copy to `deploy/.env` for `docker compose -f deploy/docker-compose.*.yml`.
# `RESEND_CLIENT_API_KEY` must be a real key when `mail_provider=resend`.
DOCKERHUB_NAMESPACE=
DB_PASSWORD=
JWT_SECRET=
RESEND_CLIENT_API_KEY=
