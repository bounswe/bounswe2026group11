# Copy to `backend/.env` for backend-only runs.
# Non-secret runtime settings live in `config/application.local.yaml` and `config/application.dev.yaml`.
# Keep only secrets here.
DB_PASSWORD=postgres
JWT_SECRET=dev-only-change-for-production
