# Remote dev droplet: pulls pre-built images from Docker Hub (:latest). No `build:` — CI pushes images separately.
#
# Paths are relative to this file. On the server, mirror the repo layout under e.g. `/opt/sem/`
# (`deploy/`, `nginx/`, `certs/` for TLS, `.env`) and run from that root:
#   docker compose -f deploy/docker-compose.dev.yml pull && docker compose -f deploy/docker-compose.dev.yml up -d
#
# Requires `.env` with DB_*, JWT_SECRET, and DOCKERHUB_NAMESPACE (see root `.env.example` and docs/deploy.md).

name: sem-dev

services:
  postgres:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 3s
      timeout: 5s
      retries: 10
      start_period: 10s
    networks:
      - app

  backend:
    image: ${DOCKERHUB_NAMESPACE}/sem-backend-dev:latest
    pull_policy: always
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      APP_PORT: "8080"
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
    networks:
      - app

  frontend:
    image: ${DOCKERHUB_NAMESPACE}/sem-frontend-dev:latest
    pull_policy: always
    networks:
      - app

  nginx:
    image: nginx:1.27-alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ../nginx/nginx.dev.conf:/etc/nginx/nginx.conf:ro
      - ../certs:/etc/nginx/certs:ro
    depends_on:
      - backend
      - frontend
    restart: unless-stopped
    networks:
      - app

volumes:
  pgdata:

networks:
  app:
